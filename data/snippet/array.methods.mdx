---
title: Array/String Methods
description: These methods cover a wide range of operations you can perform on arrays and strings
logo: /static/images/algorithm/algorithm.png
categories: ['Extra']
---

## Evolution of JavaScript Array Methods (37)

### ES1 (1997)

- `concat()`
- `indexOf()`
- `join()`
- `lastIndexOf()`
- `pop()` 🔄
- `push()` 🔄
- `reverse()` 🔄
- `shift()` 🔄
- `slice()`
- `sort()` 🔄
- `splice()` 🔄
- `toLocaleString()`
- `toString()`
- `unshift()` 🔄

### ES5 (2009)

- `every()`
- `filter()`
- `forEach()`
- `map()`
- `reduce()`
- `reduceRight()`
- `some()`

### ES2015

- `copyWithin()` 🔄
- `entries()`
- `fill()` 🔄
- `find()`
- `findIndex()`
- `keys()`
- `values()`

### ES2016

- `includes()`

### ES2019

- `flat()`
- `flatMap()`

### ES2022

- `at()`

### ES2023 (New Additions)

- `findLast()`
- `findLastIndex()`
- `toSorted()`
- `toSpliced()`
- `toReversed()`

## Evolution of JavaScript String Methods (27)

### ES1 (1997)

- `charAt()`
- `charCodeAt()`
- `concat()`
- `indexOf()`
- `lastIndexOf()`
- `slice()`
- `substring()`
- `toLowerCase()`
- `toUpperCase()`

### ES3 (1999)

- `charAt()`
- `charCodeAt()`
- `concat()`
- `indexOf()`
- `lastIndexOf()`
- `slice()`
- `substring()`
- `toLowerCase()`
- `toUpperCase()`

### ES5 (2009)

- `charAt()`
- `charCodeAt()`
- `concat()`
- `indexOf()`
- `lastIndexOf()`
- `slice()`
- `substring()`
- `toLowerCase()`
- `toUpperCase()`

### ES2015

- `charAt()`
- `charCodeAt()`
- `concat()`
- `includes()`
- `endsWith()`
- `indexOf()`
- `lastIndexOf()`
- `repeat()`
- `replace()`
- `slice()`
- `startsWith()`
- `substring()`
- `toLowerCase()`
- `toUpperCase()`
- `trim()`

### ES2017

- `padEnd()`
- `padStart()`

### ES2021

- `replaceAll()`

---
